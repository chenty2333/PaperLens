# Prompt Resources

This folder is bundled as editable runtime resource material. The MVP uses deterministic
classification; later milestones will add prompt files for the Agents SDK readers and auditors.
